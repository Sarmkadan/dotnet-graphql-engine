# ValidationHelper

`ValidationHelper` is a static utility class providing validation methods for common GraphQL-related inputs. It ensures that strings conform to expected formats for query components, identifiers, and other structured data before they are processed by the GraphQL engine. These methods are typically used during query parsing, schema validation, or input sanitization to reject malformed or potentially unsafe values early.

## API

### `ValidateQueryString`
